steps:
- checkout: self
  clean: true
  lfs: true
  submodules: false
  persistCredentials: true  # if 'true', leave the OAuth token in the Git config after the initial fetch; defaults to false
  fetchTags: false # Prevent checking out tags unnecessarily (can cause issues on Mac)