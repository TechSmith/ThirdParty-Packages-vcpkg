diff --git a/CMakeLists.txt b/CMakeLists.txt
index e103c40..dba55f5 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -179,11 +179,29 @@ if(ZLIB_BUILD_SHARED)
                                               VERSION ${INSTALL_VERSION})
     endif(NOT CYGWIN)
 
+    # Set output names based on platform
+    if(WIN32)
+        set(ZLIB_DLL_NAME zlib1)
+        set(ZLIB_IMPORT_LIB_NAME zlib)
+        set(ZLIB_PDB_NAME zlib)
+    else()
+        set(ZLIB_DLL_NAME z)
+    endif()
+
     set_target_properties(
         zlib
         PROPERTIES DEFINE_SYMBOL ZLIB_DLL
                    EXPORT_NAME ZLIB
-                   OUTPUT_NAME z)
+                   OUTPUT_NAME ${ZLIB_DLL_NAME})
+    
+    # Set Windows-specific import library and PDB names
+    # Note: CMAKE_DEBUG_POSTFIX (set to "d" on line 154) will be automatically appended to debug builds
+    if(WIN32)
+        set_target_properties(
+            zlib
+            PROPERTIES ARCHIVE_OUTPUT_NAME ${ZLIB_IMPORT_LIB_NAME}
+                       PDB_NAME ${ZLIB_PDB_NAME})
+    endif()
     if(UNIX
         AND NOT APPLE
         AND NOT (CMAKE_SYSTEM_NAME STREQUAL AIX)
